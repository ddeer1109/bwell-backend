spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    port: 5432
    host: localhost
    databaseName: bwell
    url: jdbc:postgresql://localhost:5432/bwell
    username: postgres
    password: poki123
    driverClassName: org.postgresql.Driver
  sql:
    init:
      platform: postgres
